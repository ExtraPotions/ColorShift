const fs=require('fs'),{chromium}=require('playwright'),assert=require('node:assert/strict');(async()=>{const b=await chromium.launch();try{const p=await b.newPage({viewport:{width:900,height:700}});await p.route('https://example.org/**',r=>r.fulfill({contentType:'text/html',body:'<button id="ordinary">Ordinary</button><button id="anchor" style="position:fixed;right:20px;bottom:20px;width:48px;height:48px">Help</button>'}));await p.addInitScript({content:fs.readFileSync(require("./edition-path.cjs")('colorshift-anywhere.user.js'),'utf8')});await p.goto('https://example.org');const fab=p.locator('#colorshift-fab'),anchor=p.locator('#anchor');await fab.waitFor();let a=await anchor.boundingBox(),f=await fab.boundingBox();assert(f.width>0&&f.height>0);assert(Math.abs(f.x-a.x)<=8);await anchor.evaluate(e=>{e.style.bottom='auto';e.style.top='8px'});await p.evaluate(()=>{window.dispatchEvent(new Event('resize'));window.dispatchEvent(new Event('scroll'));});await p.waitForFunction(()=>{const a=document.querySelector('#anchor').getBoundingClientRect(),f=document.querySelector('#colorshift-root').shadowRoot.querySelector('.fab').getBoundingClientRect();return f.top-a.bottom===8});assert.equal(await anchor.evaluate(e=>e.style.top),'8px');await anchor.evaluate(e=>e.remove());await p.waitForFunction(()=>{const r=document.querySelector('#colorshift-root').shadowRoot.querySelector('.fab').getBoundingClientRect();return r.top>=8&&r.bottom<=innerHeight;});console.log('Anywhere docking: 8px above/below, follows moved anchor, preserves anchor, ignores inline button and falls back after removal');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});






