const {chromium}=require('playwright'),fs=require('fs'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch();try{const p=await b.newPage();await p.route('https://example.org/**',r=>r.fulfill({contentType:'text/html',body:'<main>Sample page</main>'}));await p.addInitScript({content:fs.readFileSync(require('./edition-path.cjs')('colorshift-anywhere.user.js'),'utf8')});await p.goto('https://example.org/');assert.equal(await p.getByRole('dialog').isVisible(),false);assert.equal(await p.locator('#colorshift-fab').evaluate(e=>getComputedStyle(e,'::after').backgroundColor),'rgb(239, 68, 68)');await p.locator('#colorshift-fab').click();assert.equal(await p.getByRole('switch',{name:'Enable on this site',exact:true}).getAttribute('aria-checked'),'true');assert.equal(await p.getByRole('combobox',{includeHidden:true,name:'Theme',exact:true}).inputValue(),'system');console.log('Anywhere starts enabled, collapsed, with System reset behavior');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
